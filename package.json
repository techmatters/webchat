{
  "name": "webchat",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "cross-env MODE=development CONFIG=as-development webpack serve --open",
    "build": "webpack",
    "build:as-development": "cross-env MODE=development CONFIG=as-development npm run build",
    "build:test-staging": "cross-env MODE=production CONFIG=test-staging npm run build",
    "build:as-staging": "cross-env MODE=production CONFIG=beta npm run build",
    "build:zm-staging": "cross-env MODE=production CONFIG=zm-staging npm run build",
    "build:zm-production": "cross-env MODE=production CONFIG=zm-production npm run build",
    "build:za-staging": "cross-env MODE=production CONFIG=za-staging npm run build",
    "build:za-production": "cross-env MODE=production CONFIG=za-production npm run build",
    "build:et-staging": "cross-env MODE=production CONFIG=et-staging npm run build",
    "build:et-production": "cross-env MODE=production CONFIG=et-production npm run build",
    "build:mw-staging": "cross-env MODE=production CONFIG=mw-staging npm run build",
    "build:mw-production": "cross-env MODE=production CONFIG=mw-production npm run build",
    "build:jm-staging": "cross-env MODE=production CONFIG=jm-staging npm run build",
    "build:jm-production": "cross-env MODE=production CONFIG=jm-production npm run build",
    "build:ca-staging": "cross-env MODE=production CONFIG=ca-staging npm run build",
    "build:uk-staging": "cross-env MODE=production CONFIG=uk-staging npm run build",
    "build:ro-staging": "cross-env MODE=production CONFIG=ro-staging npm run build",
    "build:hu-staging": "cross-env MODE=production CONFIG=hu-staging npm run build",
    "build:hu-production": "cross-env MODE=production CONFIG=hu-production npm run build",
    "build:e2e-development": "cross-env MODE=development CONFIG=e2e-development npm run build",
    "build:co-staging": "cross-env MODE=production CONFIG=co-staging npm run build",
    "build:co-production": "cross-env MODE=production CONFIG=co-production npm run build",
    "build:cl-staging": "cross-env MODE=production CONFIG=cl-staging npm run build",
    "build:zw-staging": "cross-env MODE=production CONFIG=zw-staging npm run build",
    "build:pl-staging": "cross-env MODE=production CONFIG=pl-staging npm run build",
    "build:mt-staging": "cross-env MODE=production CONFIG=mt-staging npm run build",
    "build:mt-production": "cross-env MODE=production CONFIG=mt-production npm run build",
    "lint": "eslint --ext js --ext jsx --ext ts --ext tsx src/ tests/",
    "lint:fix": "npm run lint -- --fix",
    "test": "jest"
  },
  "devDependencies": {
    "@babel/core": "^7.19.0",
    "@babel/preset-env": "^7.19.0",
    "@babel/preset-react": "^7.18.6",
    "@babel/preset-typescript": "^7.18.6",
    "@types/jest": "^29.2.6",
    "@types/react-redux": "^7.1.24",
    "@typescript-eslint/parser": "^5.37.0",
    "babel-loader": "^8.2.5",
    "babel-runtime": "^6.26.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.23.1",
    "eslint-config-twilio-react": "^2.0.0",
    "html-webpack-plugin": "^5.0.0",
    "jest": "^29.3.1",
    "process": "^0.11.10",
    "raw-loader": "^4.0.2",
    "ts-jest": "^29.0.5",
    "ts-loader": "^8.0.16",
    "typescript": "^4.1.3",
    "webpack": "^5.75.0",
    "webpack-cli": "^4.5.0",
    "webpack-dev-server": "^3.11.2",
    "webpack-utf8-bom": "^1.3.0"
  },
  "dependencies": {
    "@twilio/flex-webchat-ui": "^2.9.1",
    "redux": "^4.2.0"
  },
  "prettier": {
    "endOfLine": "auto"
  }
}
